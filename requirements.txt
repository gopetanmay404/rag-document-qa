langchain
langchain-groq
python-dotenv
fastapi
uvicorn
pypdf
streamlit
streamlit
pymupdf
python-dotenv

langchain-community
langchain-huggingface
faiss-cpu
sentence-transformers

# ── Core UI ──────────────────────────────────────────────
streamlit>=1.35.0

# ── PDF Processing ────────────────────────────────────────
pymupdf>=1.24.0          # imports as 'fitz'

# ── Environment / Config ──────────────────────────────────
python-dotenv>=1.0.0

# ── LangChain Stack ───────────────────────────────────────
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-groq>=0.1.6
langchain-huggingface>=0.0.3

# ── LangGraph (Agentic RAG pipeline) ─────────────────────
langgraph>=0.1.0

# ── Vector Store ─────────────────────────────────────────
faiss-cpu>=1.7.4         # Use faiss-gpu if you have CUDA

# ── Embeddings ───────────────────────────────────────────
sentence-transformers>=2.7.0   # Powers all 3 embedding model options
torch>=2.0.0                   # Required by sentence-transformers (CPU mode)

# ── Type Hints ───────────────────────────────────────────
typing_extensions>=4.9.0       # Required by LangGraph TypedDict state
